export const MAX_IMAGE_BYTES=12*1024*1024;
export const ACCEPTED_IMAGE_TYPES=Object.freeze(["image/jpeg","image/png","image/webp"]);
export const LAYOUT_ID="war-result-ultrawide-v1";
export const SLOT_ORDER=Object.freeze(["left-1","left-2","left-3","left-4","left-5","right-1","right-2","right-3","right-4","right-5"]);
export const STRATEGIES=Object.freeze(["full_capture","grouped_wide_crops","grouped_tight_crops","individual_crop"]);
const BASE={y:.49,width:.084,height:.36,left:[.012,.112,.212,.312,.412],right:[.512,.612,.712,.812,.912]};
export function detectLayout(width,height){if(!Number.isFinite(width)||!Number.isFinite(height)||width<=0||height<=0)throw new Error("Dimensions invalides.");const ratio=width/height;if(ratio<3.75||ratio>4.25)throw new Error("Ratio non compatible avec war-result-ultrawide-v1.");return{layoutId:LAYOUT_ID,ratio};}
export function getLayoutSlots(){return SLOT_ORDER.map((slot,index)=>{const side=index<5?"left":"right",position=index%5+1;return Object.freeze({slot,label:`${side==="left"?"G":"D"}${position}`,side,position,x:BASE[side][position-1],y:BASE.y,width:BASE.width,height:BASE.height});});}
export function calculatePixelRect(slot,width,height){const x=Math.max(0,Math.round(slot.x*width)),y=Math.max(0,Math.round(slot.y*height));return{x,y,width:Math.min(width-x,Math.max(1,Math.round(slot.width*width))),height:Math.min(height-y,Math.max(1,Math.round(slot.height*height)))};}
export function getCropVariants(slot){return{wide:{...slot},tight:{...slot,x:slot.x+slot.width*.18,y:slot.y+slot.height*.02,width:slot.width*.64,height:slot.height*.62},grayscale:{...slot,filter:"grayscale"},redMask:{...slot,filter:"red-mask"}};}
export function validateUpload(file){if(!file||typeof file!=="object")throw new Error("Fichier manquant.");if(!ACCEPTED_IMAGE_TYPES.includes(file.type))throw new Error("Format non accepté.");if(!Number.isFinite(file.size)||file.size<=0)throw new Error("Fichier vide.");if(file.size>MAX_IMAGE_BYTES)throw new Error("Image supérieure à 12 Mo.");return true;}
export function detectRedCross(imageData){const{data,width,height}=imageData||{};if(!data||!width||!height)return false;let red=0;const a=new Uint16Array(width+height+1),b=new Uint16Array(width+height+1);for(let y=0;y<height;y++)for(let x=0;x<width;x++){const i=(y*width+x)*4,r=data[i],g=data[i+1],bl=data[i+2],al=data[i+3];if(al>80&&r>145&&r>g*1.35&&r>bl*1.25){red++;a[x-y+height]++;b[x+y]++;}}const min=Math.max(5,Math.round(Math.min(width,height)*.28));return Math.max(...a)>=min&&Math.max(...b)>=min&&red/(width*height)>=.035;}
export function createDraft(strategy="full_capture"){if(!STRATEGIES.includes(strategy))throw new Error("Stratégie invalide.");return{schemaVersion:"2.0.0",status:"draft",layout:LAYOUT_ID,strategy,provider:"none",groqRealCalls:0,slots:getLayoutSlots().map(s=>({slot:s.slot,side:s.side,position:s.position,barred:null,candidates:[],selectedCharacterId:null,validationStatus:"pending"}))};}
export function validateCandidate(candidate,characterIds){if(!candidate||typeof candidate!=="object"||typeof candidate.characterId!=="string")throw new Error("Candidat invalide.");if(characterIds&&!characterIds.has(candidate.characterId))throw new Error("Personnage hors catalogue.");if(candidate.confidence!=null&&(!Number.isFinite(candidate.confidence)||candidate.confidence<0||candidate.confidence>1))throw new Error("Confiance invalide.");}
export function validateDraft(draft,characterIds=null){if(!draft||draft.schemaVersion!=="2.0.0"||draft.layout!==LAYOUT_ID||!STRATEGIES.includes(draft.strategy))throw new Error("Contrat invalide.");if(!Number.isInteger(draft.groqRealCalls)||draft.groqRealCalls<0)throw new Error("Compteur Groq invalide.");if(!Array.isArray(draft.slots)||draft.slots.length!==10)throw new Error("Dix slots requis.");const seen=new Set();draft.slots.forEach((entry,index)=>{if(entry.slot!==SLOT_ORDER[index]||seen.has(entry.slot))throw new Error("Slots absents, dupliqués ou désordonnés.");seen.add(entry.slot);if(!["pending","validated","corrected"].includes(entry.validationStatus))throw new Error("Statut invalide.");if(!Array.isArray(entry.candidates)||entry.candidates.length>5)throw new Error("Candidats invalides.");entry.candidates.forEach(c=>validateCandidate(c,characterIds));if(entry.selectedCharacterId&&characterIds&&!characterIds.has(entry.selectedCharacterId))throw new Error("Personnage hors catalogue.");});return true;}
export function normalizeCatalog(catalog){if(!Array.isArray(catalog))throw new Error("Catalogue invalide.");const byId=new Map(),aliases=new Map();for(const item of catalog){if(!item?.id||!item?.nameKey)continue;byId.set(item.id,item);for(const value of[item.id,item.nameKey,item.nameFr,item.nameEn])if(value)aliases.set(normalizeText(value),item.id);}return{byId,aliases};}
export function normalizeText(value){return String(value||"").normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g,"").trim();}
export function resolveCharacter(raw,catalogIndex){if(!raw||!catalogIndex)return{status:"unknown",characterId:null};if(raw.characterId&&catalogIndex.byId.has(raw.characterId))return{status:"exact",characterId:raw.characterId};const key=normalizeText(raw.name||raw.characterId);const id=catalogIndex.aliases.get(key);return id?{status:"alias",characterId:id}:{status:"hallucination",characterId:null};}
export function calculateTopMetrics(slots,groundTruth){const truth=new Map(groundTruth.map(e=>[e.slot,e.characterId]));const out={evaluated:0,top1:0,top3:0,top5:0,barred:{evaluated:0,top1:0,top3:0,top5:0},unbarred:{evaluated:0,top1:0,top3:0,top5:0}};for(const slot of slots){const expected=truth.get(slot.slot);if(!expected||!Array.isArray(slot.candidates))continue;const truthRow=groundTruth.find(e=>e.slot===slot.slot),bucket=truthRow?.barred?out.barred:out.unbarred,ids=slot.candidates.map(c=>c.characterId);out.evaluated++;bucket.evaluated++;for(const[k,n]of[["top1",1],["top3",3],["top5",5]])if(ids.slice(0,n).includes(expected)){out[k]++;bucket[k]++;}}return out;}
